<%= form_with(model: user_team) do |form| %>
  <% if user_team.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_team.errors.count, "error") %> prohibited this user_team from being saved:</h2>

      <ul>
        <% user_team.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :team_id, style: "display: block" %>
    <%= form.text_field :team_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
