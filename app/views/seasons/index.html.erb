<p style="color: green"><%= notice %></p>

<h1>Seasons</h1>

<div id="seasons">
  <% @seasons.each do |season| %>
    <%= render season %>
    <p>
      <%= link_to "Show this season", league_season_path(@league, season) %>
    </p>
  <% end %>
</div>

<%= link_to "New season", new_league_season_path %>
