<%= form_with(model: user_league) do |form| %>
  <% if user_league.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_league.errors.count, "error") %> prohibited this user_league from being saved:</h2>

      <ul>
        <% user_league.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :league_id, style: "display: block" %>
    <%= form.text_field :league_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
