<%= form_with model: [@league, @team, team_season] do |form| %>
  <% if team_season.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(team_season.errors.count, "error") %> prohibited this team_season from being saved:</h2>

      <ul>
        <% team_season.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :season_id, style: "display: block"%>
    <%= form.collection_select :season_id, @team.league.seasons, :id, :start_year %>
    
  </div>


  <div>
    <%= form.submit %>
  </div>
<% end %>
