<%= form_with(model: league_membership) do |form| %>
  <% if league_membership.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(league_membership.errors.count, "error") %> prohibited this user_league from being saved:</h2>

      <ul>
        <% league_membership.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :member_id, style: "display: block" %>
    <%= form.collection_select :member_id, User.all, :id, :email %>
  </div>

  <div>
    <%= form.label :league_id, style: "display: block" %>
    <%= form.collection_select :league_id, League.all, :id, :name %>
  </div>

  <div>
    <%= form.label :role, style: "display: block" %>
    <%= form.select :role, ["admin", "member"] %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
